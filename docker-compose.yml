services:
  db:
    image: postgres:16-alpine
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: starter_tanstack
    volumes:
      - postgres_data_starter_tanstack:/var/lib/postgresql/data

volumes:
  postgres_data_starter_tanstack:
